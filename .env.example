# Client-side environment variables (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend (Supabase Edge Functions) secrets to configure via supabase CLI or dashboard:
# OPENAI_API_KEY=
# GEMINI_API_KEY=

# Notes:
# - Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY for the frontend to connect to Supabase.
# - Configure OPENAI_API_KEY and GEMINI_API_KEY as function secrets used by your Edge Functions (e.g., chat-with-ai).
# - Do NOT expose OpenAI/Gemini keys in the frontend; keep them in Supabase secrets only.